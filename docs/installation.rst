============
Installation
============

At the command line::

    $ easy_install django-service-status

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-service-status
    $ pip install django-service-status
